# Paus NFT MarketPlace

## How to run

You can start the project by using following command:

```shell
npm run start
```

You can change the configuration in the configuration [file](./src/configuration.ts)
